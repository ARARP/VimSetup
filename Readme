This repo is basically a duplication of my VimSetup. To 
use it :
 - cd <Some-place-for-ConfigFiles>
 - git clone https://github.com/ARARP/VimSetup 
 - ln -s VimSetup     ~/.vim
 - ln -s .vim/.vimrc  ~/.vimrc

cheers
Arun

